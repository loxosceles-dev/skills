Read the latest critic entry in `docs/planning/discussions/{topic}.md` and respond to it point by point. Don't rewrite the plan — the critic has full context. Cover two things: where and why you're pushing back, and how you intend to resolve the points you accept. If the critique surfaces a topic that needs its own design depth, note it as a separate doc to be reviewed immediately after.

Prepend your response with:

```
## YYYY-MM-DD HH:MM — Dev
```

Use the actual current timestamp. Append to the same file — never overwrite earlier entries.
